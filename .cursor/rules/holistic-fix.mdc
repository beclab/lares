---
description: 修 bug、异常行为、状态损坏或调用点特判顶住时使用；禁止补丁式修法，须从模块整体流程定位根因
alwaysApply: false
---

# 整体性修复（禁止补丁式修法）

修 bug 前先摸清模块职责与端到端流程；目标是**提升健壮性**，不是让当前症状消失。

## 必须

1. **摸清边界**：调用链、状态机、错误路径、上下游契约。
2. **定位根因**：问「为什么允许走到这里」，不是「这里怎么挡住报错」。
3. **按流程修**：在正确抽象层修正不变量、校验、生命周期或错误传播；必要时补测试。
4. **值得独立则拆出**：重试/降级、状态流转、资源供给等，提取为独立模块并用明确接口对接。

## 禁止

- 调用点堆 `if` / `try/except` / 特判「先顶住」。
- 只改表象（吞异常、硬编码兜底、复制旁路）。
- 在臃肿上帝函数里继续打补丁。

## 自检

- 去掉这处特判后，同模块其他入口是否仍正确？
- 失败是否可观测、可重试、不静默损坏状态？
- 同类问题是否仍要改 N 个补丁点？若是，继续拆模块。
